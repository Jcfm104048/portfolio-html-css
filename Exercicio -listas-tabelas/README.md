# Exercício – Listas e Tabelas em HTML

## 📌 Sobre o projeto
Projeto desenvolvido para praticar a estruturação de conteúdo em HTML, utilizando diferentes tipos de listas e tabelas, com estilização em CSS.

## 🛠 Tecnologias utilizadas
- HTML5
- CSS3

## 🎯 O que foi praticado
- Listas não ordenadas e listas aninhadas (`ul`, `li`) 
- Listas ordenadas e reversas (`ol`, `reversed`)
- Listas de definição (`dl`, `dt`, `dd`)
- Estruturação semântica (`header`, `main`, `section`, `article`)
- Tabelas HTML (`table`, `thead`, `tbody`, `tfoot`, `caption`)
- Estilização avançada de listas e tabelas com CSS.

## ▶️ Como visualizar o projeto
Abra o arquivo `Exercicio3.html` no navegador.

## 📂 Estrutura do projeto
```
Exercicio-listas/
├── Exercicio3.html
└── style.css
```